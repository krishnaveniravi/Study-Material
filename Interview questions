Edureka:
14.What is break  and continue?
break:
It breaks the loop prematurely if the condition is met.
Continue:
It is used to skip the current iteration and move to next iteration.
########################
15.Can we pass function as a argument in python?
Yes, we can pass function as a argument to other function ,bcoz function is a first class object.
def caps(text):
  return text.capitalize()
def greet(func,name):
  mgs=func(name)
  return mgs
print(greet(caps,"hari"))
#########################
16.what does # symbol do in python?
It is used to comment the line in python 
for signle line comment
#function definition
#########################
17.What is type conversion in python?
Converts one data type to another datatype:
1.int()-->converts into integer
2.float()-->converts into float
3.str()-->converts into string
4.list()-->converts into list
5.set()
6.dict()
7.bin(),oct(),hex()
##########################
18.What is docstring in python :
docstring or document string is a multiline string used to document the specific code segment.
It is comes with in triple qoutes.
sometimes acts as comment because it is not assign to any variable.
############################
19.What is slicing in python ?
Slicing in python is used to extract the specific part of teh string or list .
By specifying start,end,step
start---included
end----not included
sted----interval between the elements
##########################
20.What is pickling and unpickling?
Pickling:
Pickle module accept the python object and converts into a string or byte stream representation
and dumps into a file using dump function.

unpickling:
Retrives the original object of the python from the string representation.
load into the file using load function.
##########################
INTERMEDIATE LEVEL:
==================
21.Explain inheritance in python?
Inheritance in python allows a class(child class) to inherit the methods and attributes of the another base class(paren classs or super class) 
without no need to rewrite them.
Easier to manage and maintain the complex software system.
1.Single inheritance===>single parent single child
2.multiple inheritance===>multiple parents single child
3.hierarchy inheritance==>single parent multiple child
4.multilevel inheritance==>grand parent--parent--child
5.hybrid inheritance===>any two of the above inheritance
#########################
22.Does python makes use of access specifiers? private,public,protected
In python we can control how attributes and methods of objects are accessed.
"_"--->protected ==can be accessed within class or subclass.
"_ _"-->private == cannot be accessed by directly from the outside of the class.
They are used to restrict access and protect internal data.
public members:
Public members are variables or methods that can be accessed from anywhere inside the class,
outside the class or from other modules. By default, all members in Python are public.
##########################
23.Discuss different way to remove the elements from the list?
#remove()--->delete the mentioned element from the list
#pop()---->delete the mentioned index of element from the list
#########################
24.How are classes created ion python?
class keyword followed by class name starting with captial first letter.
__init__method is defined in starting of class.methods and attributes of class will be created as followed,
eg:
class Fruit()
  def __init__(self,name,color):
    self.name=name
    self.color=color
obj=Fruit(mango,yellow)
print(obj.name)
print(obj.color)
########################
25.write a pyhton program to excecute the bubble sort in list?
li=[7,8,9,3,2,5,6]
l=len(li)
for i in range(l):
  for j in range(0,l-i-1):
    if li[j]>li[j+1]:
      li[j],li[j+1]=li[j+1],li[j]
print(li)
#########################
26.write a code to generate the square of every element of the list?
l=[1,2,3,4,5]
res=[i**2 for i in l]
print(res)
############################
27.Write a program to produce the fibonacci series in python.
Fibonacci series is the sequence of numbers where the each number is the sum of the two preceding number.
Usually starting with 0 and 1.
def fibonacci(n):
  fib_series=[0,1]
  while len(fib_series)<n:
    fib_series.append(fib_series[-1]+fib_series[-2])
  return fib_series
nth=int(input("Enter the no.of terms:"))
print(fibonacci(8))
############################
28.Difference between shallow copy and deep copy?
shallow copy :
L1=[1,2,4,5,6]
l2=l1====>shallow copy..copying their reference.
deepcopy:
L1=[1,2,4,5,6]
l2=l1.copy() ===>deepcopy doesn't affect original list if copied list is modified
############################
29.write a program to check gn sequence is palindrome?
def is_palindrome(n):
  return n==n[::-1]

print(is_palindrome("string))
############################
30.How is multithereading is acheivedin python?
Multithreading is achieved by threading module.
which enables the concurrent execution of multiple thread in a single process.
each code can run its code and perform simultaneously.
#with threading

from threading import Thread

def print_java(): #independent function
    for i in range(10000):
        print(i, ": java")
        
def print_python(): #independent function
    for i in range(10000):
        print(i, ": python")
              
thread1 = Thread(target = print_java)
thread2 = Thread(target = print_python)

thread1.start()
thread2.start()

###########################
31.How is memory managed in python?
Python manages memory by using python heapspace where the object and datastructures are stored.
It is managed by interpreter ,programmers do not have direct access to this private heapspace.
It has a built in garbage collector to recycles the unused memory and free it up and making it available for further use.

##########################
32.Whenever python exits, why is not all the memory de allocated?
Python does deallocate ,most memory upon exit.
But some memory doesn't because of garbage collector,memory leakage and external resourse management
##########################
33.which is faster,pyhton list or NumPy arrays and why?
NumPy is faster than python list for the numerical computations and operations involving in large datasets.
Numpy written in c language( have same datatypes )and optimizedd for perfomance.
Python list are more flexible but slower due to their dynamic typing in nature and lack of optimization for numerical.
for hetrogeneous operations python list is more flexible than NumPy array.
##########################
34.What is the process of compilation and linking?
Compilation:
Process of compilation involves converting the source code into byte code that can be executed by the python interpreter.
Linking:
Linking is not as explicit and complex as in compiled languages such as c,c++.
Linking process involves in several steps related to import and usage of modules and packages.
#########################
35.How to add a new column to pandas dataframe?
import pandas as pd
d={"col1":[1,2,3],"col2":[1,2,3]}
df=pd.DataFrame(d)
df["Sum"]=df["col1"]+df["col2"]
print(df)
##########################
36.write a code to sort an Array in NumPy by the (N-1)th column?
numpy.argsort()
import numpy as np
arr=np.array([[9,2,3],[4,5,6],[1,3,1]])
sorted_arr=np.argsort(arr[:,-1])
res=arr[sorted_arr]
print(res)
#########################
37.How do you compute the Euclidean design between two series?
import numpy as np
from scipy.spatial.distance import euclidean
n1=np.array([1,2,4,5])
n2=np.array([3,4,7,8])
res=euclidean(n1,n2)
print(res)
########################
38.How do you get the items not common to both series A and Series B?
pandas.Series.isin()
import pandas pd
a=pd.Series([1,2,3,4,5])
b=pd.Series([4,5,6,7,8])

not_in_b=a[~a.isin(b)]
not_in_a=b[~b.isin(a)]

res=concat([not_in_a,not_in_b]).reset_index(drop=True)
print(res)
#########################
39.What is flask and explain its benefits?
Flask is a micro webframework written in python that requires minimal setup to get a simple application running.
Has very high flexibility.
############################
40.Is django is better tha flask?
Django is better suit for large,complex applications with many in built features.
Django fullstack framework,flexibility low
choice depends on your requirements.
############################
41.What is the use of sessions in the django framework?
The primary use of session in django framework is provide a way to store and retrieve 
user specific data across multiple,otherwise independent,HTTP request.
This is because 
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################
##########################





















  
